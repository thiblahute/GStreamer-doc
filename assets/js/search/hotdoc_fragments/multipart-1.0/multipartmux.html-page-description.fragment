fragment_downloaded_cb({"url": "multipart-1.0/multipartmux.html#page-description", "fragment": "MultipartMux uses the GstCaps of the sink pad as the Content type field for incoming buffers when muxing them to a multipart stream. Most of the time multipart streams are sequential JPEG frames. \na pipeline to mux JPEG frames per second into a multipart stream stored to a file. \n"});