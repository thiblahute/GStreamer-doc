fragment_downloaded_cb({"url": "gstreamer-vaapi-1.0/element-vaapih264enc.html#element-vaapih264enc", "fragment": "element vaapih264enc \nEncodes raw video streams into H.264 bitstreams. \nFor example the following code informs the encoder to enable ROI with a region for ROI. Note that if an application wants to disable the region just send an event with roi value and same coordination. \ngst_element_send_event pipeline event \n"});