fragment_downloaded_cb({"url": "libgstopencv-so-1.0/grabcut.html#grabcut-page", "fragment": "grabcut \nRuns Grabcut algorithm on input alpha. Values BG FG PR_BG PR_FGD NOTE larger values of alpha notably are interpreted as PR_FGD too. IN CASE OF no alpha mask input all s or all s the face downstream event is used to create a bbox of PR_FG elements. IF nothing is present then nothing is done. \n"});