fragment_downloaded_cb({"url": "gst-devtools-1.0/c/subprojects/gst-devtools/validate/gst/validate/gst-validate-enums.html#GST_VALIDATE_SHOW_MONITOR", "fragment": "If set as the default level all the distinct issues for all the monitors will be reported. If set as the level for a particular object all the distinct issues for this object will be reported. Note that if the same issue happens twice on the same object up until this level that issue is only reported once. \n"});