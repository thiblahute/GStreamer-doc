fragment_downloaded_cb({"url": "plugins-doc-1.0/capsfilter.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"capsfilter\">\n<h1 id=\"capsfilter1\">capsfilter</h1><p>The element does not modify data as such, but can enforce limitations on the\ndata format.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code>gst-launch-1.0 videotestsrc ! capsfilter caps=video/x-raw,format=GRAY8 ! videoconvert ! autovideosink\n</code></pre>\n<p>Limits acceptable video from videotestsrc to be grayscale. Equivalent to</p>\n<pre><code>gst-launch-1.0 videotestsrc ! video/x-raw,format=GRAY8 ! videoconvert ! autovideosink\n</code></pre>\n<p>which is a short notation for the capsfilter element.</p>\n\n</div>\n\n\n"});