fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-1.0/element-speed.html#speed1", "fragment": "speed \nPlays an audio stream at a different speed by resampling the audio \nDo not use this element. Either use the pitch element or do a seek with a non rate parameter this will have the same effect as using the speed element but relies on the decoder demuxer to handle this correctly also requires a fairly up to date gst plugins base as of February \n"});