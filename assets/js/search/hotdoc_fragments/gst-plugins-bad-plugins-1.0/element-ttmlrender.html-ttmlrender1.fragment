fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-1.0/element-ttmlrender.html#ttmlrender1", "fragment": "ttmlrender \nRenders timed text on top of a video stream. It receives text in buffers from a ttmlparse element each text string is in its own GstMemory within the GstBuffer and the styling and layout associated with each text string is in metadata attached to the GstBuffer. \n"});