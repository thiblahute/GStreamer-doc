fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-1.0/element-avwait.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-avwait\">\n<h1 id=\"avwait\">avwait</h1><p>This element will drop all buffers until a specific timecode or running\ntime has been reached. It will then pass-through both audio and video,\nstarting from that specific timecode or running time, making sure that\naudio starts as early as possible after the video (or at the same time as\nthe video). In the \"audio-after-video\" mode, it only drops audio buffers\nuntil video has started.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 filesrc location=\"my_file\" ! decodebin name=d ! \"audio/x-raw\" ! avwait name=l target-timecode-str=\"00:00:04:00\" ! autoaudiosink d. ! \"video/x-raw\" ! timecodestamper ! l. l. ! queue ! timeoverlay time-mode=time-code ! autovideosink\n</code></pre>\n\n</div>\n\n\n        "});