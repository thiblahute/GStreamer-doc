fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-1.0/element-irtspparse.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-irtspparse\">\n<h1 id=\"irtspparse1\">irtspparse</h1><p>This is an interleaved RTSP parser that allows extracting specific\nso-called \"channels\" from received interleaved (TCP) RTSP data\n(typically extracted from some network capture).</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 filesrc location=h264crasher.pcap ! pcapparse ! irtspparse\n ! rtph264depay ! ffdec_h264 ! fakesink\n ]| Read from a pcap dump file using filesrc, extract the raw TCP packets,\n depayload and decode them.\n</code></pre>\n\n</div>\n\n\n"});