fragment_downloaded_cb({"url": "gst-editing-services-1.0/ges-timeline-element.html#ges_timeline_element_set_parent", "fragment": "ges_timeline_element_set_parent \nGES.TimelineElement.set_parent \nGES.TimelineElement.SetParent \nGES.prototype.set_parent \nSets the parent of self to parent. The parents needs to already own a hard reference on self. \nParameters \na GESTimelineElement \nnew parent of self \nTRUE if parent could be set or FALSE when self already had a parent or self and parent are the same. \nSets the parent of self to parent. The parents needs to already own a hard reference on self. \nParameters \na GES.TimelineElement \nnew parent of self \nTrue if parent could be set or False when self already had a parent or self and parent are the same. \nSets the parent of self to parent. The parents needs to already own a hard reference on self. \nParameters \nnew parent of self \ntrue if parent could be set or false when self already had a parent or self and parent are the same. \nSets the parent of self to parent. The parents needs to already own a hard reference on self. \nParameters \na GES.TimelineElement \nnew parent of self \ntrue if parent could be set or false when self already had a parent or self and parent are the same. \n"});