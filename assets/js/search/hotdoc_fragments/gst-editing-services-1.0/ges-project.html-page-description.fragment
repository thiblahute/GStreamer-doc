fragment_downloaded_cb({"url": "gst-editing-services-1.0/ges-project.html#page-description", "fragment": "The GESProject is used to control a set of GESAsset and is a GESAsset with GES_TYPE_TIMELINE as extractable_type itself. That means that you can extract GESTimeline from a project as followed \nThe GESProject class offers a higher level API to handle GESAsset s. It lets you request new asset and it informs you about new assets through a set of signals. Also it handles problem such as missing files missing GstElement and lets you try to recover from those. \n"});