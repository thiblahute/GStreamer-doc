fragment_downloaded_cb({"url": "gst-editing-services-1.0/ges-layer.html#ges_layer_is_empty", "fragment": "ges_layer_is_empty \nges_layer_is_empty \nges_layer_is_empty \nConvenience method to check if layer is empty doesn t contain any clip or not. \nParameters \nThe GESLayer to check \nTRUE if layer is empty FALSE if it already contains at least one GESClip \nConvenience method to check if layer is empty doesn t contain any clip or not. \nParameters \nThe GESLayer to check \nTRUE if layer is empty FALSE if it already contains at least one GESClip \nConvenience method to check if layer is empty doesn t contain any clip or not. \nParameters \nThe GESLayer to check \nTRUE if layer is empty FALSE if it already contains at least one GESClip \n"});