fragment_downloaded_cb({"url": "gst-plugins-base-audio-1.0/gstaudioclock.html#gst_audio_clock_new", "fragment": "GstAudio.AudioClock \nGstAudio.AudioClock \nGstAudio.AudioClock \nGstAudio.AudioClock \nCreate a new Gst.Audio.AudioClock instance. Whenever the clock time should be calculated it will call func with user_data. When func returns Gst.Constants.CLOCK_TIME_NONE the clock will return the last reported time. \nParameters \nthe name of the clock \na function \nuser data \nGLib.DestroyNotify for user_data \na new Gst.Audio.AudioClock casted to a Gst.Clock. \nCreate a new Gst.Audio.AudioClock instance. Whenever the clock time should be calculated it will call func with user_data. When func returns Gst.Constants.CLOCK_TIME_NONE the clock will return the last reported time. \nParameters \nthe name of the clock \na function \nuser data \nGLib.DestroyNotify for user_data \na new Gst.Audio.AudioClock casted to a Gst.Clock. \nCreate a new Gst.Audio.AudioClock instance. Whenever the clock time should be calculated it will call func with user_data. When func returns Gst.Constants.CLOCK_TIME_NONE the clock will return the last reported time. \nParameters \nthe name of the clock \na function \nuser data \nGLib.DestroyNotify for user_data \na new Gst.Audio.AudioClock casted to a Gst.Clock. \nCreate a new Gst.Audio.AudioClock instance. Whenever the clock time should be calculated it will call func with user_data. When func returns Gst.Constants.CLOCK_TIME_NONE the clock will return the last reported time. \nParameters \nthe name of the clock \na function \nuser data \nGLib.DestroyNotify for user_data \na new Gst.Audio.AudioClock casted to a Gst.Clock. \n"});