fragment_downloaded_cb({"url": "gst-plugins-base-audio-1.0/audio-converter.html#gst_audio_converter_update_config", "fragment": "Gst.Audio.AudioConverter.UpdateConfig \nGst.Audio.AudioConverter.UpdateConfig \nGst.Audio.AudioConverter.UpdateConfig \nGst.Audio.AudioConverter.UpdateConfig \nSet in_rate out_rate and config as extra configuration for convert. \nin_rate and out_rate specify the new sample rates of input and output formats. A value of leaves the sample rate unchanged. \nconfig can be null in which case the current configuration is not changed. \nIf the parameters in config can not be set exactly this function returns false and will try to update as much state as possible. The new state can then be retrieved and refined with Gst.Audio.AudioConverter.GetConfig. \nLook at the GST_AUDIO_CONVERTER_OPT_ fields to check valid configuration option and values. \nParameters \na Gst.Audio.AudioConverter \ninput rate \noutput rate \na Gst.Structure or null \ntrue when the new parameters could be set \nSet in_rate out_rate and config as extra configuration for convert. \nin_rate and out_rate specify the new sample rates of input and output formats. A value of leaves the sample rate unchanged. \nconfig can be null in which case the current configuration is not changed. \nIf the parameters in config can not be set exactly this function returns false and will try to update as much state as possible. The new state can then be retrieved and refined with Gst.Audio.AudioConverter.GetConfig. \nLook at the GST_AUDIO_CONVERTER_OPT_ fields to check valid configuration option and values. \nParameters \na Gst.Audio.AudioConverter \ninput rate \noutput rate \na Gst.Structure or null \ntrue when the new parameters could be set \nSet in_rate out_rate and config as extra configuration for convert. \nin_rate and out_rate specify the new sample rates of input and output formats. A value of leaves the sample rate unchanged. \nconfig can be null in which case the current configuration is not changed. \nIf the parameters in config can not be set exactly this function returns false and will try to update as much state as possible. The new state can then be retrieved and refined with Gst.Audio.AudioConverter.GetConfig. \nLook at the GST_AUDIO_CONVERTER_OPT_ fields to check valid configuration option and values. \nParameters \ninput rate \noutput rate \na Gst.Structure or null \ntrue when the new parameters could be set \nSet in_rate out_rate and config as extra configuration for convert. \nin_rate and out_rate specify the new sample rates of input and output formats. A value of leaves the sample rate unchanged. \nconfig can be null in which case the current configuration is not changed. \nIf the parameters in config can not be set exactly this function returns false and will try to update as much state as possible. The new state can then be retrieved and refined with Gst.Audio.AudioConverter.GetConfig. \nLook at the GST_AUDIO_CONVERTER_OPT_ fields to check valid configuration option and values. \nParameters \na Gst.Audio.AudioConverter \ninput rate \noutput rate \na Gst.Structure or null \ntrue when the new parameters could be set \n"});