fragment_downloaded_cb({"url": "gst-plugins-base-audio-1.0/gstaudiodecoder.html#gst_audio_decoder_get_latency", "fragment": "Gst.Audio.AudioDecoder.GetLatency \nGst.Audio.AudioDecoder.GetLatency \nGst.Audio.AudioDecoder.GetLatency \nGst.Audio.AudioDecoder.GetLatency \nSets the variables pointed to by min and max to the currently configured latency. \nParameters \na Gst.Audio.AudioDecoder \nSets the variables pointed to by min and max to the currently configured latency. \nParameters \na Gst.Audio.AudioDecoder \na pointer to storage to hold minimum latency \na pointer to storage to hold maximum latency \nSets the variables pointed to by min and max to the currently configured latency. \nParameters \na pointer to storage to hold minimum latency \na pointer to storage to hold maximum latency \nSets the variables pointed to by min and max to the currently configured latency. \nParameters \na Gst.Audio.AudioDecoder \n"});