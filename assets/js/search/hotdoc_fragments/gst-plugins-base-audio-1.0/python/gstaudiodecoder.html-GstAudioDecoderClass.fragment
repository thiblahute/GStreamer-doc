fragment_downloaded_cb({"url": "gst-plugins-base-audio-1.0/python/gstaudiodecoder.html#GstAudioDecoderClass", "fragment": "GstAudioDecoderClass.start \nOptional. Called when the element starts processing. Allows opening external resources. \nGstAudioDecoderClass.parse not introspectable \nOptional. Allows chopping incoming data into manageable units frames for subsequent decoding. This division is at subclass discretion and may or may not correspond to or more frames as defined by audio format. \nGstAudioDecoderClass.flush \nOptional. Instructs subclass to clear any codec caches and discard any pending samples and not yet returned decoded data. hard indicates whether a FLUSH is being processed or otherwise a DISCONT or conceptually similar \nGstAudioDecoderClass.src_event \nOptional. Event handler on the src pad. Subclasses should chain up to the parent implementation to invoke the default handler. \nGstAudioDecoderClass.open \nOptional. Called when the element changes to GST_STATE_READY. Allows opening external resources. \nGstAudioDecoderClass.decide_allocation \nOptional. Setup the allocation parameters for allocating output buffers. The passed in query contains the result of the downstream allocation query. Subclasses should chain up to the parent implementation to invoke the default handler. \nGstAudioDecoderClass.propose_allocation \nOptional. Propose buffer allocation parameters for upstream elements. Subclasses should chain up to the parent implementation to invoke the default handler. \nGstAudioDecoderClass.sink_query \nOptional. Query handler on the sink pad. This function should return TRUE if the query could be performed. Subclasses should chain up to the parent implementation to invoke the default handler. Since \nGstAudioDecoderClass.src_query \nOptional. Query handler on the source pad. This function should return TRUE if the query could be performed. Subclasses should chain up to the parent implementation to invoke the default handler. Since \nGstAudioDecoderClass.getcaps \nOptional. Allows for a custom sink getcaps implementation. If not implemented default returns gst_audio_decoder_proxy_getcaps applied to sink template caps. \nGstAudioDecoderClass._gst_reserved \n"});