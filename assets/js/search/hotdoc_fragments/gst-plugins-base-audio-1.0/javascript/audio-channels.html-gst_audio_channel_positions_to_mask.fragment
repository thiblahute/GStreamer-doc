fragment_downloaded_cb({"url": "gst-plugins-base-audio-1.0/javascript/audio-channels.html#gst_audio_channel_positions_to_mask", "fragment": "GstAudio.prototype.audio_channel_positions_to_mask \nConvert the position array of channels channels to a bitmask. \nIf force_order is true it additionally checks if the channels are in the order required by GStreamer. \nposition \nThe GstAudio.AudioChannelPosition \nchannels \nThe number of channels. \nforce_order \nOnly consider the GStreamer channel order. \n\ntrue if the channel positions are valid and could be converted. \nchannel_mask \ntrue if the channel positions are valid and could be converted. \n"});