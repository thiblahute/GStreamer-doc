fragment_downloaded_cb({"url": "gst-plugins-good-doc-1.0/autoaudiosrc.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"autoaudiosrc\">\n<h1 id=\"autoaudiosrc1\">autoaudiosrc</h1><p>autoaudiosrc is an audio source that automatically detects an appropriate\naudio source to use.  It does so by scanning the registry for all elements\nthat have <quote>Source</quote> and <quote>Audio</quote> in the class field\nof their element information, and also have a non-zero autoplugging rank.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code>gst-launch-1.0 -v -m autoaudiosrc ! audioconvert ! audioresample ! autoaudiosink\n</code></pre>\n\n</div>\n\n\n"});