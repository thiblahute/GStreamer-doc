fragment_downloaded_cb({"url": "gst-plugins-good-doc-1.0/wavpackdec.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"wavpackdec\">\n<h1 id=\"wavpackdec1\">wavpackdec</h1><p>WavpackDec decodes framed (for example by the WavpackParse element)\nWavpack streams and decodes them to raw audio.\n<ulink url=\"http://www.wavpack.com/\">Wavpack</ulink> is an open-source\naudio codec that features both lossless and lossy encoding.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code>gst-launch-1.0 filesrc location=test.wv ! wavpackparse ! wavpackdec ! audioconvert ! audioresample ! autoaudiosink\n</code></pre>\n<p>This pipeline decodes the Wavpack file test.wv into raw audio buffers and\ntries to play it back using an automatically found audio sink.</p>\n\n</div>\n\n\n"});