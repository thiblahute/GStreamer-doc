fragment_downloaded_cb({"url": "gst-plugins-good-doc-1.0/element-matroskamux.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-matroskamux\">\n<h1 id=\"matroskamux1\">matroskamux</h1><p>matroskamux muxes different input streams into a Matroska file.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 -v filesrc location=/path/to/mp3 ! mpegaudioparse ! matroskamux name=mux ! filesink location=test.mkv  filesrc location=/path/to/theora.ogg ! oggdemux ! theoraparse ! mux.\n ]| This pipeline muxes an MP3 file and a Ogg Theora video into a Matroska file.\n |[\n gst-launch-1.0 -v audiotestsrc num-buffers=100 ! audioconvert ! vorbisenc ! matroskamux ! filesink location=test.mka\n ]| This pipeline muxes a 440Hz sine wave encoded with the Vorbis codec into a Matroska file.\n</code></pre>\n\n</div>\n\n\n"});