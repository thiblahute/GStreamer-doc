fragment_downloaded_cb({"url": "gst-plugins-good-doc-1.0/element-autoaudiosink.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-autoaudiosink\">\n<h1 id=\"autoaudiosink1\">autoaudiosink</h1><p>autoaudiosink is an audio sink that automatically detects an appropriate\naudio sink to use.  It does so by scanning the registry for all elements\nthat have <quote>Sink</quote> and <quote>Audio</quote> in the class field\nof their element information, and also have a non-zero autoplugging rank.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 -v -m audiotestsrc ! audioconvert ! audioresample ! autoaudiosink\n</code></pre>\n\n</div>\n\n\n"});