fragment_downloaded_cb({"url": "gst-plugins-good-doc-1.0/autovideosink.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"autovideosink\">\n<h1 id=\"autovideosink1\">autovideosink</h1><p>autovideosink is a video sink that automatically detects an appropriate\nvideo sink to use.  It does so by scanning the registry for all elements\nthat have <quote>Sink</quote> and <quote>Video</quote> in the class field\nof their element information, and also have a non-zero autoplugging rank.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code>gst-launch-1.0 -v -m videotestsrc ! autovideosink\n</code></pre>\n\n</div>\n\n\n"});