fragment_downloaded_cb({"url": "gst-plugins-good-doc-1.0/element-smpte.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-smpte\">\n<h1 id=\"smpte1\">smpte</h1><p>smpte can accept I420 video streams with the same width, height and\nframerate. The two incomming buffers are blended together using an effect\nspecific alpha mask.</p>\n<p>The GstSmpte:depth property defines the presision in bits of the mask. A\nhigher presision will create a mask with smoother gradients in order to avoid\nbanding.</p>\n<h2 id=\"sample-pipelines\">Sample pipelines</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 -v videotestsrc pattern=1 ! smpte name=s border=20000 type=234 duration=2000000000 ! videoconvert ! ximagesink videotestsrc ! s.\n ]| A pipeline to demonstrate the smpte transition.\n It shows a pinwheel transition a from a snow videotestsrc to an smpte\n pattern videotestsrc. The transition will take 2 seconds to complete. The\n edges of the transition are smoothed with a 20000 big border.\n</code></pre>\n\n</div>\n\n\n"});