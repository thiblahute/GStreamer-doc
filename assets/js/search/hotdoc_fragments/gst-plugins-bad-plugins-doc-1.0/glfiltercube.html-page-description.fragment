fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-doc-1.0/glfiltercube.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"glfiltercube\">\n<h1 id=\"glfiltercube1\">glfiltercube</h1><p>The resize and redraw callbacks can be set from a client code.</p>\n<h2 id=\"examples\">Examples</h2>\n<pre><code>gst-launch-1.0 -v videotestsrc ! glfiltercube ! glimagesink\n</code></pre>\n<p>A pipeline to mpa textures on the 6 cube faces..\nFBO is required.</p>\n<pre><code>gst-launch-1.0 -v videotestsrc ! glfiltercube ! video/x-raw, width=640, height=480 ! glimagesink\n</code></pre>\n<p>Resize scene after drawing the cube.\nThe scene size is greater than the input video size.</p>\n<pre><code>gst-launch-1.0 -v videotestsrc ! video/x-raw, width=640, height=480  ! glfiltercube ! glimagesink\n</code></pre>\n<p>Resize scene before drawing the cube.\nThe scene size is greater than the input video size.</p>\n\n</div>\n\n\n"});