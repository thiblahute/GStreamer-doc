fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-doc-1.0/chromahold.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"chromahold\">\n<h1 id=\"chromahold1\">chromahold</h1><p>The chromahold element will remove all color information for\nall colors except a single one and converts them to grayscale.</p>\n<p>Sample pipeline:</p>\n<pre><code>gst-launch-1.0 videotestsrc pattern=smpte75 ! \\\n  chromahold target-r=0 target-g=0 target-b=255 ! \\\n  videoconvert ! autovideosink     \\\n</code></pre>\n<p>This pipeline only keeps the red color.</p>\n\n</div>\n\n\n"});