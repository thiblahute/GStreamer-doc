fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-doc-1.0/katedec.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"katedec\">\n<h1 id=\"katedec1\">katedec</h1><p>This element decodes Kate streams\n<ulink url=\"http://libkate.googlecode.com/\">Kate</ulink> is a free codec\nfor text based data, such as subtitles. Any number of kate streams can be\nembedded in an Ogg stream.</p>\n<p>libkate (see above url) is needed to build this plugin.</p>\n<h2 id=\"example-pipeline\">Example pipeline</h2>\n<p>This explicitely decodes a Kate stream:</p>\n<pre><code>gst-launch-1.0 filesrc location=test.ogg ! oggdemux ! katedec ! fakesink silent=TRUE\n</code></pre>\n<p>This will automatically detect and use any Kate streams multiplexed\nin an Ogg stream:</p>\n<pre><code>gst-launch-1.0 playbin uri=file:///tmp/test.ogg\n</code></pre>\n\n</div>\n\n\n"});