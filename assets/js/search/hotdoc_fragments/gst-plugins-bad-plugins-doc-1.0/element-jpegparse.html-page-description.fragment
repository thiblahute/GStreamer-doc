fragment_downloaded_cb({"url": "gst-plugins-bad-plugins-doc-1.0/element-jpegparse.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-jpegparse\">\n<h1 id=\"jpegparse1\">jpegparse</h1><p>Parses a JPEG stream into JPEG images.  It looks for EOI boundaries to\nsplit a continuous stream into single-frame buffers. Also reads the\nimage header searching for image properties such as width and height\namong others. Jpegparse can also extract metadata (e.g. xmp).</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 -v souphttpsrc location=... ! jpegparse ! matroskamux ! filesink location=...\n</code></pre>\n<p>The above pipeline fetches a motion JPEG stream from an IP camera over\nHTTP and stores it in a matroska file.</p>\n\n</div>\n\n\n"});