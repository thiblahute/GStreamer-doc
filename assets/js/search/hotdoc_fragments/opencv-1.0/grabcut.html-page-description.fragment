fragment_downloaded_cb({"url": "opencv-1.0/grabcut.html#page-description", "fragment": "Runs Grabcut algorithm on input alpha. Values BG FG PR_BG PR_FGD NOTE larger values of alpha notably are interpreted as PR_FGD too. IN CASE OF no alpha mask input all s or all s the face downstream event is used to create a bbox of PR_FG elements. IF nothing is present then nothing is done. \n"});