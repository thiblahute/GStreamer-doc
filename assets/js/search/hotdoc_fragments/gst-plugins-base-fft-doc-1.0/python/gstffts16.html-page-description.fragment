fragment_downloaded_cb({"url": "gst-plugins-base-fft-doc-1.0/python/gstffts16.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"gstffts16.h\">\n<h1 id=\"gstffts16\">GstFFTS16</h1><p>GstFFTS16 provides a FFT implementation and related functions for\nsigned 16 bit integer samples. To use this call <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_new\">gst_fft_s16_new (not introspectable)</a> for\nallocating a GstFFTS16 instance with the appropriate parameters and\nthen call <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_fft\">GstFft.FFTS16.fft</a> or <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_inverse_fft\">GstFft.FFTS16.inverse_fft</a> to perform the\nFFT or inverse FFT on a buffer of samples.</p>\n<p>After use free the GstFFTS16 instance with <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_free\">GstFft.FFTS16.free</a>.</p>\n<p>For the best performance use <a href=\"../../gst-plugins-base-fft-doc-1.0/gstfft.html#gst_fft_next_fast_length\">GstFft.fft_next_fast_length</a> to get a\nnumber that is entirely a product of 2, 3 and 5 and use this as the\n<em>len</em> parameter for <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_new\">gst_fft_s16_new (not introspectable)</a>.</p>\n<p>The <em>len</em> parameter specifies the number of samples in the time domain that\nwill be processed or generated. The number of samples in the frequency domain\nis <em>len</em>/2 + 1. To get n samples in the frequency domain use 2*n - 2 as <em>len</em>.</p>\n<p>Before performing the FFT on time domain data it usually makes sense\nto apply a window function to it. For this <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_window\">GstFft.FFTS16.window</a> can comfortably\nbe used.</p>\n<p>Be aware, that you can't simply run <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_inverse_fft\">GstFft.FFTS16.inverse_fft</a> on the\nresulting frequency data of <a href=\"../../gst-plugins-base-fft-doc-1.0/gstffts16.html#gst_fft_s16_fft\">GstFft.FFTS16.fft</a> to get the original data back.\nThe relation between them is iFFT (FFT (x)) = x / nfft where nfft is the\nlength of the FFT. This also has to be taken into account when calculation\nthe magnitude of the frequency data.</p>\n\n</div>\n\n\n"});