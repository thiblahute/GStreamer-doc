fragment_downloaded_cb({"url": "gst-plugins-base-pbutils-1.0/gstdiscoverer.html#gst_discoverer_new", "fragment": "GstPbutils.Discoverer \nGstPbutils.Discoverer \nGstPbutils.Discoverer \nGstPbutils.Discoverer \nCreates a new Gst.PbUtils.Discoverer with the provided timeout. \nParameters \ntimeout per file in nanoseconds. Allowed are values between one second GST_SECOND and one hour Gst.Constants.SECOND \nThe new Gst.PbUtils.Discoverer. If an error occurred when creating the discoverer err will be set accordingly and null will be returned. If err is set the caller must free it when no longer needed using GLib.Error.Free. \nCreates a new Gst.PbUtils.Discoverer with the provided timeout. \nParameters \ntimeout per file in nanoseconds. Allowed are values between one second GST_SECOND and one hour Gst.Constants.SECOND \nThe new Gst.PbUtils.Discoverer. If an error occurred when creating the discoverer err will be set accordingly and null will be returned. If err is set the caller must free it when no longer needed using GLib.Error.Free. \nCreates a new Gst.PbUtils.Discoverer with the provided timeout. \nParameters \ntimeout per file in nanoseconds. Allowed are values between one second GST_SECOND and one hour Gst.Constants.SECOND \nThe new Gst.PbUtils.Discoverer. If an error occurred when creating the discoverer err will be set accordingly and null will be returned. If err is set the caller must free it when no longer needed using GLib.Error.Free. \nCreates a new Gst.PbUtils.Discoverer with the provided timeout. \nParameters \ntimeout per file in nanoseconds. Allowed are values between one second GST_SECOND and one hour Gst.Constants.SECOND \nThe new Gst.PbUtils.Discoverer. If an error occurred when creating the discoverer err will be set accordingly and null will be returned. If err is set the caller must free it when no longer needed using GLib.Error.Free. \n"});