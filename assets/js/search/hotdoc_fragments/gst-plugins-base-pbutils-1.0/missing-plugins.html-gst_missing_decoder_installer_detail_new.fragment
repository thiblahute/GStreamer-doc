fragment_downloaded_cb({"url": "gst-plugins-base-pbutils-1.0/missing-plugins.html#gst_missing_decoder_installer_detail_new", "fragment": "Gst.PbUtils.Global.MissingDecoderInstallerDetailNew \nGst.PbUtils.Global.MissingDecoderInstallerDetailNew \nGst.PbUtils.Global.MissingDecoderInstallerDetailNew \nGst.PbUtils.Global.MissingDecoderInstallerDetailNew \nReturns an opaque string containing all the details about the missing element to be passed to an external installer called via Gst.PbUtils.Global.InstallPluginsAsync or Gst.PbUtils.Global.InstallPluginsSync. \nThis function is mainly for applications that call external plugin installation mechanisms using one of the two above mentioned functions in the case where the application knows exactly what kind of plugin it is missing. \nParameters \nthe fixed caps for which a decoder element is needed \na newly allocated detail string or NULL on error. Free string with GLib.Global.Free when not needed any longer. \nReturns an opaque string containing all the details about the missing element to be passed to an external installer called via Gst.PbUtils.Global.InstallPluginsAsync or Gst.PbUtils.Global.InstallPluginsSync. \nThis function is mainly for applications that call external plugin installation mechanisms using one of the two above mentioned functions in the case where the application knows exactly what kind of plugin it is missing. \nParameters \nthe fixed caps for which a decoder element is needed \na newly allocated detail string or NULL on error. Free string with GLib.Global.Free when not needed any longer. \nReturns an opaque string containing all the details about the missing element to be passed to an external installer called via Gst.PbUtils.Global.InstallPluginsAsync or Gst.PbUtils.Global.InstallPluginsSync. \nThis function is mainly for applications that call external plugin installation mechanisms using one of the two above mentioned functions in the case where the application knows exactly what kind of plugin it is missing. \nParameters \nthe fixed caps for which a decoder element is needed \na newly allocated detail string or NULL on error. Free string with GLib.Global.Free when not needed any longer. \nReturns an opaque string containing all the details about the missing element to be passed to an external installer called via Gst.PbUtils.Global.InstallPluginsAsync or Gst.PbUtils.Global.InstallPluginsSync. \nThis function is mainly for applications that call external plugin installation mechanisms using one of the two above mentioned functions in the case where the application knows exactly what kind of plugin it is missing. \nParameters \nthe fixed caps for which a decoder element is needed \na newly allocated detail string or NULL on error. Free string with GLib.Global.Free when not needed any longer. \n"});