fragment_downloaded_cb({"url": "libgstreamer-1.0/gstclock.html#gst_clock_set_synced", "fragment": "Gst.prototype.set_synced \nGst.Clock.set_synced \nGst.Clock.is_synced \ngst_clock_set_synced \nSets clock to synced and emits the GstClock synced signal and wakes up any thread waiting in Gst.prototype.wait_for_sync. \nThis function must only be called if GST_CLOCK_FLAG_NEEDS_STARTUP_SYNC is set on the clock and is intended to be called by subclasses only. \nParameters \na GstClock \nif the clock is synced \nSince \nSets clock to synced and emits the GstClock synced signal and wakes up any thread waiting in Gst.Clock.wait_for_sync. \nThis function must only be called if GST_CLOCK_FLAG_NEEDS_STARTUP_SYNC is set on the clock and is intended to be called by subclasses only. \nParameters \na GstClock \nif the clock is synced \nSince \nSets clock to synced and emits the GstClock synced signal and wakes up any thread waiting in Gst.Clock.WaitForSync. \nThis function must only be called if GST_CLOCK_FLAG_NEEDS_STARTUP_SYNC is set on the clock and is intended to be called by subclasses only. \nParameters \nif the clock is synced \nSince \nSets clock to synced and emits the GstClock synced signal and wakes up any thread waiting in gst_clock_wait_for_sync. \nThis function must only be called if GST_CLOCK_FLAG_NEEDS_STARTUP_SYNC is set on the clock and is intended to be called by subclasses only. \nParameters \na GstClock \nif the clock is synced \nSince \n"});