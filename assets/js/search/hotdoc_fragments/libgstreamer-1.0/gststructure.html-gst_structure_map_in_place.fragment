fragment_downloaded_cb({"url": "libgstreamer-1.0/gststructure.html#gst_structure_map_in_place", "fragment": "gst_structure_map_in_place \nGst.Structure.map_in_place \nGst.Structure.MapInPlace \nGst.prototype.map_in_place \nCalls the provided function once for each field in the GstStructure. In contrast to gst_structure_foreach the function may modify but not delete the fields. The structure must be mutable. \nParameters \na GstStructure \na function to call for each field \nprivate data \nTRUE if the supplied function returns TRUE For each of the fields FALSE otherwise. \nCalls the provided function once for each field in the Gst.Structure. In contrast to Gst.Structure.foreach the function may modify but not delete the fields. The structure must be mutable. \nParameters \na Gst.Structure \na function to call for each field \nprivate data \nTrue if the supplied function returns True For each of the fields False otherwise. \nCalls the provided function once for each field in the Gst.Structure. In contrast to Gst.Structure.Foreach the function may modify but not delete the fields. The structure must be mutable. \nParameters \na function to call for each field \nprivate data \ntrue if the supplied function returns true For each of the fields false otherwise. \nCalls the provided function once for each field in the Gst.Structure. In contrast to Gst.prototype.foreach the function may modify but not delete the fields. The structure must be mutable. \nParameters \na Gst.Structure \na function to call for each field \nprivate data \ntrue if the supplied function returns true For each of the fields false otherwise. \n"});