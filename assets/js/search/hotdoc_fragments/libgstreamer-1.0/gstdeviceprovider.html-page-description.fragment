fragment_downloaded_cb({"url": "libgstreamer-1.0/gstdeviceprovider.html#page-description", "fragment": "A GstDeviceProvider subclass is provided by a plugin that handles devices if there is a way to programatically list connected devices. It can also optionally provide updates to the list of connected devices. \nEach GstDeviceProvider subclass is a singleton a plugin should normally provide a single subclass for all devices. \nApplications would normally use a GstDeviceMonitor to monitor devices from all relevant providers. \n"});