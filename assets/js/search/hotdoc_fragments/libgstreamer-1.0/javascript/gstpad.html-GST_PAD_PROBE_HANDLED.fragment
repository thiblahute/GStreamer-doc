fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstpad.html#GST_PAD_PROBE_HANDLED", "fragment": "Data has been handled in the probe and will not be forwarded further. For events and buffers this is the same behaviour as Gst.PadProbeReturn.prototype.drop except that in this case you need to unref the buffer or event yourself For queries it will also return true to the caller. The probe can also modify the Gst.FlowReturn value by using the Gst.prototype.PAD_PROBE_INFO_FLOW_RETURN accessor. Note that the resulting query must contain valid entries. Since \n"});