fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstquery.html#gstqueryprototypeset_segment", "fragment": "Answer a segment query by setting the requested values. The normal playback segment of a pipeline is to duration at the default rate of If a seek was performed on the pipeline to play a different segment this query will return the range specified in the last seek. \nstart_value and stop_value will respectively contain the configured playback range start and stop values expressed in format. The values are always between and the duration of the media and start_value stop_value. rate will contain the playback rate. For negative rates playback will actually happen from stop_value to start_value. \n"});