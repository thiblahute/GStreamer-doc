fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstbuffer.html#gst_buffer_is_memory_range_writable", "fragment": "Gst.Buffer.prototype.is_memory_range_writable \nCheck if length memory blocks in buffer starting from idx are writable. \nlength can be to check all the memory blocks after idx. \nNote that this function does not check if buffer is writable use Gst.prototype.buffer_is_writable to check that if needed. \n"});