fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstevent.html#gsteventprototypeparse_tag", "fragment": "Parses a tag event and stores the results in the given taglist location. No reference to the taglist will be returned it remains valid only until the event is freed. Don t modify or free the taglist make a copy if you want to modify it or store it for later use. \n"});