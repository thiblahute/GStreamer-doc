fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gsttoc.html#GST_TOC_SCOPE_CURRENT", "fragment": "TOC for the currently active selected stream this is a TOC representing the current stream from start to EOS and is what a TOC writer muxer is usually interested in it will usually be a subset of the global TOC e.g. just the chapters of the current title or the chapters selected for playback from the current title \n"});