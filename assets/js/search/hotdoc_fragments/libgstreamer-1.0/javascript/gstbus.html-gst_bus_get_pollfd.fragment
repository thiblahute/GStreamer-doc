fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstbus.html#gst_bus_get_pollfd", "fragment": "Gst.Bus.prototype.get_pollfd \nGets the file descriptor from the bus which can be used to get notified about messages being available with functions like GLib.prototype.poll and allows integration into other event loops based on file descriptors. Whenever a message is available the POLLIN GLib.IOCondition.prototype.in event is set. \nWarning NEVER read or write anything to the returned fd but only use it for getting notifications via GLib.prototype.poll or similar and then use the normal GstBus API e.g. Gst.Bus.prototype.pop. \nParameters \nA Gst.Bus \nA GPollFD to fill \nSince \n"});