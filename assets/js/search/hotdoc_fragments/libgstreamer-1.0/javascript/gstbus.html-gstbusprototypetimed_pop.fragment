fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstbus.html#gstbusprototypetimed_pop", "fragment": "Gst.Bus.prototype.timed_pop \nGet a message from the bus waiting up to the specified timeout. \nIf timeout is this function behaves like Gst.Bus.prototype.pop. If timeout is Gst.CLOCK_TIME_NONE this function will block forever until a message was posted on the bus. \nParameters \na Gst.Bus to pop \na timeout \nthe Gst.Message that is on the bus after the specified timeout or null if the bus is empty after the timeout expired. The message is taken from the bus and needs to be unreffed with gst_message_unref not introspectable after usage. \nMT safe. \n"});