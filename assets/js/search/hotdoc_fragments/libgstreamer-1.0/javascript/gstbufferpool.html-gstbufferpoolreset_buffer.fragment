fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstbufferpool.html#gstbufferpoolreset_buffer", "fragment": "GstBufferPool reset_buffer \nreset the buffer to its state when it was freshly allocated. The default implementation will clear the flags timestamps and will remove the metadata without the Gst.MetaFlags.prototype.pooled flag even the metadata with Gst.MetaFlags.prototype.locked If the Gst.BufferFlags.prototype.tag_memory was set this function can also try to restore the memory and clear the Gst.BufferFlags.prototype.tag_memory again. \nParameters \n"});