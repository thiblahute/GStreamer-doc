fragment_downloaded_cb({"url": "libgstreamer-1.0/javascript/gstpad.html#GstPadLinkCheck-wpr", "fragment": "Check if the pads are compatible by using their template caps. This is much faster than GST_PAD_LINK_CHECK_CAPS but would be unsafe e.g. if one pad has GST_CAPS_ANY not introspectable \nDisables pushing a reconfigure event when pads are linked. \n"});