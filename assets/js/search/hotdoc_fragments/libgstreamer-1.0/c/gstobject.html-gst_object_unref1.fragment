fragment_downloaded_cb({"url": "libgstreamer-1.0/c/gstobject.html#gst_object_unref1", "fragment": "Decrements the reference count on object. If reference count hits zero destroy object. This function does not take the lock on object as it relies on atomic refcounting. \nThe unref method should never be called with the LOCK held since this might deadlock the dispose function. \n"});