fragment_downloaded_cb({"url": "libgstreamer-1.0/c/gstpad.html#returns4", "fragment": "\nan id or if no probe is pending. The id can be used to remove the probe with gst_pad_remove_probe. When using GST_PAD_PROBE_TYPE_IDLE it can happen that the probe can be run immediately and if the probe returns GST_PAD_PROBE_REMOVE this functions returns \nMT safe. \n"});