fragment_downloaded_cb({"url": "libgstreamer-1.0/c/gstsegment.html#GST_SEEK_FLAG_SNAP_NEAREST", "fragment": "go to a position near the requested position if GST_SEEK_FLAG_KEY_UNIT this means the keyframe closest to the requested position if both keyframes are at an equal distance behaves like GST_SEEK_FLAG_SNAP_BEFORE. \nGST_SEEK_FLAG_TRICKMODE_KEY_UNITS \n"});