fragment_downloaded_cb({"url": "libgstreamer-1.0/c/gsturi.html#gst_uri_get_location1", "fragment": "Extracts the location out of a given valid URI ie. the protocol and are stripped from the URI which means that the location returned includes the hostname if one is specified. The returned string must be freed using g_free. \nFree function g_free \n"});