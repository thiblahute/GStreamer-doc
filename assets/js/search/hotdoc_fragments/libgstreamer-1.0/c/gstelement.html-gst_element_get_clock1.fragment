fragment_downloaded_cb({"url": "libgstreamer-1.0/c/gstelement.html#gst_element_get_clock1", "fragment": "Gets the currently configured clock of the element. This is the clock as was last set with gst_element_set_clock. \nElements in a pipeline will only have their clock set when the pipeline is in the PLAYING state. \n"});