fragment_downloaded_cb({"url": "libgstreamer-1.0/gstpreset.html#gst_preset_is_editable", "fragment": "Gst.prototype.is_editable \nGst.IPreset.IsEditable \nGst.IPreset.IsEditable \nGst.Preset.is_editable \nCheck if one can add new presets change existing ones and remove presets. \nParameters \na GObject.Object that implements Gst.Preset \ntrue if presets are editable or false if they are static \nSince \nCheck if one can add new presets change existing ones and remove presets. \nParameters \na GLib.Object that implements Gst.IPreset \ntrue if presets are editable or false if they are static \nSince \nCheck if one can add new presets change existing ones and remove presets. \ntrue if presets are editable or false if they are static \nSince \nCheck if one can add new presets change existing ones and remove presets. \nParameters \na GObject.Object that implements Gst.Preset \nTrue if presets are editable or False if they are static \nSince \n"});