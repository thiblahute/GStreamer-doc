fragment_downloaded_cb({"url": "libgstreamer-1.0/gstcaps.html#gst_caps_new_empty", "fragment": "Gst.prototype.new_empty \nGst.Caps.new_empty \nGst.Caps \ngst_caps_new_empty \nCreates a new Gst.Caps that is empty. That is the returned Gst.Caps contains no media formats. The Gst.Caps is guaranteed to be writable. Caller is responsible for unreffing the returned caps. \nthe new Gst.Caps \nCreates a new Gst.Caps that is empty. That is the returned Gst.Caps contains no media formats. The Gst.Caps is guaranteed to be writable. Caller is responsible for unreffing the returned caps. \nthe new Gst.Caps \nCreates a new Gst.Caps that is empty. That is the returned Gst.Caps contains no media formats. The Gst.Caps is guaranteed to be writable. Caller is responsible for unreffing the returned caps. \nthe new Gst.Caps \nCreates a new GstCaps that is empty. That is the returned GstCaps contains no media formats. The GstCaps is guaranteed to be writable. Caller is responsible for unreffing the returned caps. \nthe new GstCaps \n"});