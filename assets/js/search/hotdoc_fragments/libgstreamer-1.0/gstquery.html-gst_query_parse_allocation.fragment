fragment_downloaded_cb({"url": "libgstreamer-1.0/gstquery.html#gst_query_parse_allocation", "fragment": "Gst.prototype.parse_allocation \nGst.Query.parse_allocation \nGst.Query.ParseAllocation \ngst_query_parse_allocation \nParse an allocation query writing the requested caps in caps and whether a pool is needed in need_pool if the respective parameters are non NULL. \nPool details can be retrieved using Gst.prototype.get_n_allocation_pools and Gst.prototype.parse_nth_allocation_pool. \nParameters \na Gst.Query \nParse an allocation query writing the requested caps in caps and whether a pool is needed in need_pool if the respective parameters are non NULL. \nPool details can be retrieved using Gst.Query.get_n_allocation_pools and Gst.Query.parse_nth_allocation_pool. \nParameters \na Gst.Query \nParse an allocation query writing the requested caps in caps and whether a pool is needed in need_pool if the respective parameters are non NULL. \nPool details can be retrieved using Gst.Query.GetNAllocationPools and Gst.Query.ParseNthAllocationPool. \nParameters \nThe Gst.Caps \nWhether a Gst.BufferPool is needed \nParse an allocation query writing the requested caps in caps and whether a pool is needed in need_pool if the respective parameters are non NULL. \nPool details can be retrieved using gst_query_get_n_allocation_pools and gst_query_parse_nth_allocation_pool. \nParameters \na GstQuery \nThe GstCaps \nWhether a GstBufferPool is needed \n"});