fragment_downloaded_cb({"url": "libgstreamer-1.0/gstpad.html#GstPadProbeType", "fragment": "Gst.PadProbeType \nMembers \nGstPadProbeType \nMembers \nGst.PadProbeType \nMembers \nGst.PadProbeType \nMembers \nThe different probing types that can occur. When either one of GST_PAD_PROBE_TYPE_IDLE or GST_PAD_PROBE_TYPE_BLOCK is used the probe will be a blocking probe. \nThe different probing types that can occur. When either one of GST_PAD_PROBE_TYPE_IDLE or GST_PAD_PROBE_TYPE_BLOCK is used the probe will be a blocking probe. \nThe different probing types that can occur. When either one of GST_PAD_PROBE_TYPE_IDLE or GST_PAD_PROBE_TYPE_BLOCK is used the probe will be a blocking probe. \nThe different probing types that can occur. When either one of GST_PAD_PROBE_TYPE_IDLE or GST_PAD_PROBE_TYPE_BLOCK is used the probe will be a blocking probe. \n"});