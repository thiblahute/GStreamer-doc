fragment_downloaded_cb({"url": "libgstreamer-1.0/gstbufferpool.html#gst_buffer_pool_is_active", "fragment": "Gst.prototype.is_active \nGst.BufferPool.is_active \nGst.BufferPool.IsActive \ngst_buffer_pool_is_active \nCheck if pool is active. A pool can be activated with the Gst.prototype.set_active call. \nParameters \na Gst.BufferPool \ntrue when the pool is active. \nCheck if pool is active. A pool can be activated with the Gst.BufferPool.set_active call. \nParameters \na Gst.BufferPool \nTrue when the pool is active. \nCheck if pool is active. A pool can be activated with the Gst.BufferPool.SetActive call. \ntrue when the pool is active. \nCheck if pool is active. A pool can be activated with the gst_buffer_pool_set_active call. \nParameters \na GstBufferPool \nTRUE when the pool is active. \n"});