fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstbin.html#gstbinfind_unlinked_pad", "fragment": "Recursively looks for elements with an unlinked pad of the given direction within the specified bin and returns an unlinked pad if one is found or None otherwise. If a pad is found the caller owns a reference to it and should use Gst.Object.unref on the pad when it is not needed any longer. \n"});