fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstbuffer.html#gst_buffer_is_all_memory_writable", "fragment": "Gst.Buffer.is_all_memory_writable \nCheck if all memory blocks in buffer are writable. \nNote that this function does not check if buffer is writable use Gst.buffer_is_writable to check that if needed. \n"});