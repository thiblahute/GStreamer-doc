fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstmessage.html#GST_MESSAGE_CLOCK_LOST", "fragment": "The current clock as selected by the pipeline became unusable. The pipeline will select a new clock on the next PLAYING state change. The application should set the pipeline to PAUSED and back to PLAYING when this message is received. \n"});