fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gsttaglist.html#gsttaglistget_sample_index", "fragment": "Gets the sample that is at the given index for the given tag in the given list and copies it into the variable pointed to by sample. Free the sample with gst_sample_unref not introspectable when it is no longer needed. You can retrieve the buffer from the sample using Gst.Sample.get_buffer and the associated caps if any with Gst.Sample.get_caps. \nFree function gst_sample_unref \n"});