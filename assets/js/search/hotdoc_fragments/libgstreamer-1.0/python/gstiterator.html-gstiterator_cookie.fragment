fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstiterator.html#gstiterator_cookie", "fragment": "Gst.ITERATOR_COOKIE \nMacro to get the cookie of a Gst.Iterator. The cookie of the iterator is the value of the master cookie when the iterator was created. Whenever the iterator is iterated the value is compared to the value of the master cookie. If they are different a concurrent modification happened to the iterator and a resync is needed. \n"});