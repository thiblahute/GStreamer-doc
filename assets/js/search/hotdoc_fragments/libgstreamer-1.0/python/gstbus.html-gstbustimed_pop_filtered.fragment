fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstbus.html#gstbustimed_pop_filtered", "fragment": "Gst.Bus.timed_pop_filtered \nGet a message from the bus whose type matches the message type mask types waiting up to the specified timeout and discarding any messages that do not match the mask provided \nIf timeout is this function behaves like Gst.Bus.pop_filtered. If timeout is Gst.CLOCK_TIME_NONE this function will block forever until a matching message was posted on the bus. \n"});