fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstmessage.html#GST_MESSAGE_EXTENDED", "fragment": "Message is an extended message type see below These extended message IDs can t be used directly with mask based API like Gst.Bus.poll or Gst.Bus.timed_pop_filtered but you can still filter for GST_MESSAGE_EXTENDED and then check the result for the specific type. Since \n"});