fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gsttask.html#gsttaskjoin", "fragment": "Joins task. After this call it is safe to unref the task and clean up the lock set with Gst.Task.set_lock. \nThe task will automatically be stopped with this call. \nThis function cannot be called from within a task function as this would cause a deadlock. The function will detect this and print a g_warning. \n"});