fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstelement-h.html#GST_STATE_CHANGE_READY_TO_READY", "fragment": "state change from READY to READY This might happen when going to PAUSED asynchronously failed in that case elements should make sure they are in a proper coherent READY state. Since \n"});