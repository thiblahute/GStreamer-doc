fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstsegment.html#GST_SEEK_FLAG_SNAP_NEAREST", "fragment": "go to a position near the requested position if Gst.SeekFlags.key_unit this means the keyframe closest to the requested position if both keyframes are at an equal distance behaves like Gst.SeekFlags.snap_before. \n"});