fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gststructure.html#gststructureget_array", "fragment": "Gst.Structure.get_array \nThis is useful in language bindings where unknown GValue types are not supported. This function will convert the GST_TYPE_ARRAY not introspectable and GST_TYPE_LIST not introspectable into a newly allocated GValueArray and return it through array. Be aware that this is slower then getting the GValue directly. \n"});