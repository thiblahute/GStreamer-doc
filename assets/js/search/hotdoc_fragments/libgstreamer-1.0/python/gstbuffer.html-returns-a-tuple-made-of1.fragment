fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstbuffer.html#returns-a-tuple-made-of1", "fragment": "\nTrue when size bytes starting from offset could be found in buffer and idx length and skip will be filled. \nidx \nTrue when size bytes starting from offset could be found in buffer and idx length and skip will be filled. \nlength \nTrue when size bytes starting from offset could be found in buffer and idx length and skip will be filled. \nskip \nTrue when size bytes starting from offset could be found in buffer and idx length and skip will be filled. \n"});