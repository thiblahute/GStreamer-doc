fragment_downloaded_cb({"url": "libgstreamer-1.0/python/gstcaps.html#gstcapssimplify", "fragment": "Gst.Caps.simplify \nConverts the given caps into a representation that represents the same set of formats but in a simpler form. Component structures that are identical are merged. Component structures that have values that can be merged are also merged. \nThis function takes ownership of caps and will call gst_caps_make_writable not introspectable on it if necessary so you must not use caps afterwards unless you keep an additional reference to it with gst_caps_ref. \nThis method does not preserve the original order of caps. \n"});