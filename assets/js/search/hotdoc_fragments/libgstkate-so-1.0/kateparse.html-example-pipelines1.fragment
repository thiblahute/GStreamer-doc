fragment_downloaded_cb({"url": "libgstkate-so-1.0/kateparse.html#example-pipelines1", "fragment": "This pipeline shows that the streamheader is set in the caps and that each buffer has the timestamp duration offset and offset_end set. \nThis pipeline shows remuxing. kate remuxed.ogg might not be exactly the same as kate.ogg but they should produce exactly the same decoded data. \n"});