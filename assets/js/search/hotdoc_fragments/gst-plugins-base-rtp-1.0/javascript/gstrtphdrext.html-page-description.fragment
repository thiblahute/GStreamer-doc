fragment_downloaded_cb({"url": "gst-plugins-base-rtp-1.0/javascript/gstrtphdrext.html#page-description", "fragment": "Reads the NTP time from the size NTP extension bytes in data and store the result in ntptime. \nReads the NTP time from the size NTP extension bytes in data and store the result in ntptime. \nWrites the NTP time in ntptime to the format required for the NTP header extension. data must hold at least GstRtp.RTP_HDREXT_NTP_56_SIZE bytes. \nWrites the NTP time in ntptime to the format required for the NTP header extension. data must hold at least GstRtp.RTP_HDREXT_NTP_64_SIZE bytes. \n"});