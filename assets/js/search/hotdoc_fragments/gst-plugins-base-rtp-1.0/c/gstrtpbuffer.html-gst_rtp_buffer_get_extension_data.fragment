fragment_downloaded_cb({"url": "gst-plugins-base-rtp-1.0/c/gstrtpbuffer.html#gst_rtp_buffer_get_extension_data", "fragment": "gst_rtp_buffer_get_extension_data \nGet the extension data. bits will contain the extension bits of custom data. data will point to the data in the extension and wordlen will contain the length of data in bits words. \nIf buffer did not contain an extension this function will return FALSE with bits data and wordlen unchanged. \nParameters \nthe RTP packet \nlocation for result bits \nlocation for data \nlocation for length of data in bits words \nTRUE if buffer had the extension bit set. \n"});