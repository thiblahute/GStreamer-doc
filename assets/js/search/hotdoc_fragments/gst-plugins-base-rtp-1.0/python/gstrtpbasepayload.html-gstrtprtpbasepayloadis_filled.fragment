fragment_downloaded_cb({"url": "gst-plugins-base-rtp-1.0/python/gstrtpbasepayload.html#gstrtprtpbasepayloadis_filled", "fragment": "GstRtp.RTPBasePayload.is_filled \nCheck if the packet with size and duration would exceed the configured maximum size. \npayload \na GstRtp.RTPBasePayload \nsize \nthe size of the packet \nduration \nthe duration of the packet \n\nTrue if the packet of size and duration would exceed the configured MTU or max_ptime. \n"});