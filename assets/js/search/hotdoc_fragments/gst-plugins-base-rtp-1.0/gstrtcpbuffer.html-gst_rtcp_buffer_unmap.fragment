fragment_downloaded_cb({"url": "gst-plugins-base-rtp-1.0/gstrtcpbuffer.html#gst_rtcp_buffer_unmap", "fragment": "Gst.Rtp.RTCPBuffer.Unmap \nGst.Rtp.RTCPBuffer.Unmap \nGst.Rtp.RTCPBuffer.Unmap \nGst.Rtp.RTCPBuffer.Unmap \nFinish rtcp after being constructed. This function is usually called after Gst.Rtp.Global.RtcpBufferMap and after adding the RTCP items to the new buffer. \nThe function adjusts the size of rtcp with the total length of all the added packets. \nParameters \na buffer with an RTCP packet \nFinish rtcp after being constructed. This function is usually called after Gst.Rtp.Global.RtcpBufferMap and after adding the RTCP items to the new buffer. \nThe function adjusts the size of rtcp with the total length of all the added packets. \nParameters \na buffer with an RTCP packet \nFinish rtcp after being constructed. This function is usually called after Gst.Rtp.Global.RtcpBufferMap and after adding the RTCP items to the new buffer. \nThe function adjusts the size of rtcp with the total length of all the added packets. \nFinish rtcp after being constructed. This function is usually called after Gst.Rtp.Global.RtcpBufferMap and after adding the RTCP items to the new buffer. \nThe function adjusts the size of rtcp with the total length of all the added packets. \nParameters \na buffer with an RTCP packet \n"});