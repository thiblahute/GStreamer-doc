fragment_downloaded_cb({"url": "gst-plugins-base-rtp-1.0/gstrtcpbuffer.html#gst_rtcp_buffer_map", "fragment": "Gst.Rtp.Global.RtcpBufferMap \nGst.Rtp.Global.RtcpBufferMap \nGst.Rtp.Global.RtcpBufferMap \nGst.Rtp.Global.RtcpBufferMap \nOpen buffer for reading or writing depending on flags. The resulting RTCP buffer state is stored in rtcp. \nParameters \na buffer with an RTCP packet \nflags for the mapping \nresulting Gst.Rtp.RTCPBuffer \nOpen buffer for reading or writing depending on flags. The resulting RTCP buffer state is stored in rtcp. \nParameters \na buffer with an RTCP packet \nflags for the mapping \nresulting Gst.Rtp.RTCPBuffer \nOpen buffer for reading or writing depending on flags. The resulting RTCP buffer state is stored in rtcp. \nParameters \na buffer with an RTCP packet \nflags for the mapping \nresulting Gst.Rtp.RTCPBuffer \nOpen buffer for reading or writing depending on flags. The resulting RTCP buffer state is stored in rtcp. \nParameters \na buffer with an RTCP packet \nflags for the mapping \nresulting Gst.Rtp.RTCPBuffer \n"});