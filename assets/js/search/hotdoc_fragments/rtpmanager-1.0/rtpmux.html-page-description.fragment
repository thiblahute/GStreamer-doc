fragment_downloaded_cb({"url": "rtpmanager-1.0/rtpmux.html#page-description", "fragment": "The rtp muxer takes multiple RTP streams having the same clock rate and muxes into a single stream with a single SSRC. \nIn this example an audio stream is captured from ALSA and another is generated both are encoded into different payload types and muxed together so they can be sent on the same port. \n"});