fragment_downloaded_cb({"url": "libgstbase-1.0/gstbasesink.html#gst_base_sink_set_drop_out_of_segment", "fragment": "GstBase.prototype.set_drop_out_of_segment \nGstBase.BaseSink.set_drop_out_of_segment \ngst_base_sink_set_drop_out_of_segment \nConfigure sink to drop buffers which are outside the current segment \nParameters \nthe sink \ndrop buffers outside the segment \nSince \nConfigure sink to drop buffers which are outside the current segment \nParameters \nthe sink \ndrop buffers outside the segment \nSince \nConfigure sink to drop buffers which are outside the current segment \nParameters \nthe sink \ndrop buffers outside the segment \nSince \n"});