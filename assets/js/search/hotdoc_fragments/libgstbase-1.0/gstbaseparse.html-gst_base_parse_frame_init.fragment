fragment_downloaded_cb({"url": "libgstbase-1.0/gstbaseparse.html#gst_base_parse_frame_init", "fragment": "GstBase.prototype.init \ngst_base_parse_frame_init \ngst_base_parse_frame_init \ngst_base_parse_frame_init \nSets a GstBase.BaseParseFrame to initial state. Currently this means all public fields are zero ed and a private flag is set to make sure GstBase.prototype.free only frees the contents but not the actual frame. Use this function to initialise a GstBase.BaseParseFrame allocated on the stack. \nParameters \nGstBase.BaseParseFrame. \nSets a GstBaseParseFrame to initial state. Currently this means all public fields are zero ed and a private flag is set to make sure gst_base_parse_frame_free only frees the contents but not the actual frame. Use this function to initialise a GstBaseParseFrame allocated on the stack. \nParameters \nGstBaseParseFrame. \nSets a GstBaseParseFrame to initial state. Currently this means all public fields are zero ed and a private flag is set to make sure gst_base_parse_frame_free only frees the contents but not the actual frame. Use this function to initialise a GstBaseParseFrame allocated on the stack. \nSets a GstBaseParseFrame to initial state. Currently this means all public fields are zero ed and a private flag is set to make sure gst_base_parse_frame_free only frees the contents but not the actual frame. Use this function to initialise a GstBaseParseFrame allocated on the stack. \nParameters \nGstBaseParseFrame. \n"});