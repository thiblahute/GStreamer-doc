fragment_downloaded_cb({"url": "libgstbase-1.0/gstbaseparse.html#gst_base_parse_set_duration", "fragment": "GstBase.prototype.set_duration \nGstBase.BaseParse.set_duration \ngst_base_parse_set_duration \nSets the duration of the currently playing media. Subclass can use this when it is able to determine duration and or notices a change in the media duration. Alternatively if interval is non zero default then stream duration is determined based on estimated bitrate and updated every interval frames. \nParameters \nGstBase.BaseParse. \nGst.Format. \nduration value. \nhow often to update the duration estimate based on bitrate or \nSets the duration of the currently playing media. Subclass can use this when it is able to determine duration and or notices a change in the media duration. Alternatively if interval is non zero default then stream duration is determined based on estimated bitrate and updated every interval frames. \nParameters \nGstBase.BaseParse. \nGst.Format. \nduration value. \nhow often to update the duration estimate based on bitrate or \nSets the duration of the currently playing media. Subclass can use this when it is able to determine duration and or notices a change in the media duration. Alternatively if interval is non zero default then stream duration is determined based on estimated bitrate and updated every interval frames. \nParameters \nGstBaseParse. \nGstFormat. \nduration value. \nhow often to update the duration estimate based on bitrate or \n"});