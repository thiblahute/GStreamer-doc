fragment_downloaded_cb({"url": "libgstbase-1.0/python/gstbasesink.html#gst_base_sink_get_drop_out_of_segment", "fragment": "GstBase.BaseSink.get_drop_out_of_segment \nChecks if sink is currently configured to drop buffers which are outside the current segment \nsink \nthe sink \n\nTrue if the sink is configured to drop buffers outside the current segment. \nSince \n"});