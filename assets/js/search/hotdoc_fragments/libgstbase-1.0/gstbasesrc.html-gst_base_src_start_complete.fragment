fragment_downloaded_cb({"url": "libgstbase-1.0/gstbasesrc.html#gst_base_src_start_complete", "fragment": "GstBase.prototype.start_complete \ngst_base_src_start_complete \ngst_base_src_start_complete \ngst_base_src_start_complete \nComplete an asynchronous start operation. When the subclass overrides the start method it should call GstBase.prototype.start_complete when the start operation completes either from the same thread or from an asynchronous helper thread. \nParameters \nbase source instance \na Gst.FlowReturn \nComplete an asynchronous start operation. When the subclass overrides the start method it should call gst_base_src_start_complete when the start operation completes either from the same thread or from an asynchronous helper thread. \nParameters \nbase source instance \na GstFlowReturn \nComplete an asynchronous start operation. When the subclass overrides the start method it should call gst_base_src_start_complete when the start operation completes either from the same thread or from an asynchronous helper thread. \nParameters \na GstFlowReturn \nComplete an asynchronous start operation. When the subclass overrides the start method it should call gst_base_src_start_complete when the start operation completes either from the same thread or from an asynchronous helper thread. \nParameters \nbase source instance \na GstFlowReturn \n"});