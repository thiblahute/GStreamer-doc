fragment_downloaded_cb({"url": "gst-plugins-base-tag-1.0/tags.html#gst_tag_parse_extended_comment", "fragment": "Gst.Tag.TagParseExtendedComment \nGst.Tag.TagParseExtendedComment \nGst.Tag.TagParseExtendedComment \nGst.Tag.TagParseExtendedComment \nConvenience function to parse a GST_TAG_EXTENDED_COMMENT string and separate it into its components. \nIf successful key lang and or value will be set to newly allocated strings that you need to free with GLib.Global.Free when done. key and lang may also be set to NULL by this function if there is no key or no language code in the extended comment string. \nParameters \nan extended comment string see Gst.Constants.TAG_EXTENDED_COMMENT \nreturn location for the comment description key or NULL \nreturn location for the comment ISO language code or NULL \nreturn location for the actual comment string or NULL \nwhether to fail if strings are not in key value form \nTRUE if the string could be parsed otherwise FALSE \nConvenience function to parse a GST_TAG_EXTENDED_COMMENT string and separate it into its components. \nIf successful key lang and or value will be set to newly allocated strings that you need to free with GLib.Global.Free when done. key and lang may also be set to NULL by this function if there is no key or no language code in the extended comment string. \nParameters \nan extended comment string see Gst.Constants.TAG_EXTENDED_COMMENT \nreturn location for the comment description key or NULL \nreturn location for the comment ISO language code or NULL \nreturn location for the actual comment string or NULL \nwhether to fail if strings are not in key value form \nTRUE if the string could be parsed otherwise FALSE \nConvenience function to parse a GST_TAG_EXTENDED_COMMENT string and separate it into its components. \nIf successful key lang and or value will be set to newly allocated strings that you need to free with GLib.Global.Free when done. key and lang may also be set to NULL by this function if there is no key or no language code in the extended comment string. \nParameters \nan extended comment string see Gst.Constants.TAG_EXTENDED_COMMENT \nreturn location for the comment description key or NULL \nreturn location for the comment ISO language code or NULL \nreturn location for the actual comment string or NULL \nwhether to fail if strings are not in key value form \nTRUE if the string could be parsed otherwise FALSE \nConvenience function to parse a GST_TAG_EXTENDED_COMMENT string and separate it into its components. \nIf successful key lang and or value will be set to newly allocated strings that you need to free with GLib.Global.Free when done. key and lang may also be set to NULL by this function if there is no key or no language code in the extended comment string. \nParameters \nan extended comment string see Gst.Constants.TAG_EXTENDED_COMMENT \nreturn location for the comment description key or NULL \nreturn location for the comment ISO language code or NULL \nreturn location for the actual comment string or NULL \nwhether to fail if strings are not in key value form \nTRUE if the string could be parsed otherwise FALSE \n"});