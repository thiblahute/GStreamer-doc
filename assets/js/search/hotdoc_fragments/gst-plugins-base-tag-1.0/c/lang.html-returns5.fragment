fragment_downloaded_cb({"url": "gst-plugins-base-tag-1.0/c/lang.html#returns5", "fragment": "\nlanguage name in UTF format or NULL if language_code could not be mapped to a language name. The returned string must not be modified and does not need to freed it will stay valid until the application is terminated. \n"});