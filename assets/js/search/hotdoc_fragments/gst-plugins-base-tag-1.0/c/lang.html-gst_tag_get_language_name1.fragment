fragment_downloaded_cb({"url": "gst-plugins-base-tag-1.0/c/lang.html#gst_tag_get_language_name1", "fragment": "gst_tag_get_language_name \nReturns the name of the language given an ISO language code as found in a GST_TAG_LANGUAGE_CODE tag. The name will be translated according to the current locale if the library was built against the iso codes package otherwise the English name will be returned \nLanguage codes are case sensitive and expected to be lower case. \nParameters \ntwo or three letter ISO language code \nlanguage name in UTF format or NULL if language_code could not be mapped to a language name. The returned string must not be modified and does not need to freed it will stay valid until the application is terminated. \n"});