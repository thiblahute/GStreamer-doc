fragment_downloaded_cb({"url": "gst-plugins-base-tag-1.0/gstexiftag.html#gst_tag_list_from_exif_buffer", "fragment": "Gst.Tag.TagListFromExifBuffer \nGst.Tag.TagListFromExifBuffer \nGst.Tag.TagListFromExifBuffer \nGst.Tag.TagListFromExifBuffer \nParses the IFD and IFD tags data contained in the buffer and puts it on a taglist. The base_offset is used to subtract from the offset in the tag entries and be able to get the offset relative to the buffer start \nParameters \nThe exif buffer \nbyte order of the data \nOffset from the tiff header to this buffer \nThe parsed taglist \nParses the IFD and IFD tags data contained in the buffer and puts it on a taglist. The base_offset is used to subtract from the offset in the tag entries and be able to get the offset relative to the buffer start \nParameters \nThe exif buffer \nbyte order of the data \nOffset from the tiff header to this buffer \nThe parsed taglist \nParses the IFD and IFD tags data contained in the buffer and puts it on a taglist. The base_offset is used to subtract from the offset in the tag entries and be able to get the offset relative to the buffer start \nParameters \nThe exif buffer \nbyte order of the data \nOffset from the tiff header to this buffer \nThe parsed taglist \nParses the IFD and IFD tags data contained in the buffer and puts it on a taglist. The base_offset is used to subtract from the offset in the tag entries and be able to get the offset relative to the buffer start \nParameters \nThe exif buffer \nbyte order of the data \nOffset from the tiff header to this buffer \nThe parsed taglist \n"});