fragment_downloaded_cb({"url": "gst-plugins-base-tag-1.0/javascript/gstexiftag.html#page-description", "fragment": "Parses the IFD and IFD tags data contained in the buffer and puts it on a taglist. The base_offset is used to subtract from the offset in the tag entries and be able to get the offset relative to the buffer start \nParses the exif tags starting with a tiff header structure. \nFormats the tags in taglist on exif format. The resulting buffer contains the tags IFD and is followed by the data pointed by the tag entries. \nFormats the tags in taglist into exif structure a tiff header is put in the beginning of the buffer. \n"});