fragment_downloaded_cb({"url": "libgstcheck-1.0/gstcheck.html#gst_check_drop_buffers", "fragment": "prototype.check_drop_buffers \ngst_check_drop_buffers \ngst_check_drop_buffers \nUnref and remove all buffers that are in the global buffers GList emptying the list. \nUnref and remove all buffers that are in the global buffers GList emptying the list. \nUnref and remove all buffers that are in the global buffers GList emptying the list. \n"});