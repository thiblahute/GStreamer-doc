fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/c/gstappsrc.html#gst_app_src_set_callbacks1", "fragment": "Set callbacks which will be executed when data is needed enough data has been collected or when a seek should be performed. This is an alternative to using the signals it has lower overhead and is thus less expensive but also less flexible. \nIf callbacks are installed no signals will be emitted for performance reasons. \n"});