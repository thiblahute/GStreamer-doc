fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/gstappsink.html#gst_app_sink_get_drop", "fragment": "GstApp.prototype.get_drop \ngst_app_sink_get_drop \ngst_app_sink_get_drop \nCheck if appsink will drop old buffers when the maximum amount of queued buffers is reached. \nParameters \na GstApp.AppSink \ntrue if appsink is dropping old buffers when the queue is filled. \nCheck if appsink will drop old buffers when the maximum amount of queued buffers is reached. \nParameters \na GstAppSink \nTRUE if appsink is dropping old buffers when the queue is filled. \nCheck if appsink will drop old buffers when the maximum amount of queued buffers is reached. \nParameters \na GstAppSink \nTRUE if appsink is dropping old buffers when the queue is filled. \n"});