fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/javascript/gstappsink.html#gst_app_sink_get_emit_signals", "fragment": "GstApp.AppSink.prototype.get_emit_signals \nCheck if appsink will emit the new preroll and new sample signals. \nappsink \na GstApp.AppSink \n\ntrue if appsink is emiting the new preroll and new sample signals. \n"});