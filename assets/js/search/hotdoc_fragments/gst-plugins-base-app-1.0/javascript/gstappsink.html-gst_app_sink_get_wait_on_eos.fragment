fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/javascript/gstappsink.html#gst_app_sink_get_wait_on_eos", "fragment": "GstApp.AppSink.prototype.get_wait_on_eos \nCheck if appsink will wait for all buffers to be consumed when an EOS is received. \nappsink \na GstApp.AppSink \n\ntrue if appsink will wait for all buffers to be consumed when an EOS is received. \n"});