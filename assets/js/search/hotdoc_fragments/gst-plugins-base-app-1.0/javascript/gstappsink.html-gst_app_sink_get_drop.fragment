fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/javascript/gstappsink.html#gst_app_sink_get_drop", "fragment": "GstApp.AppSink.prototype.get_drop \nCheck if appsink will drop old buffers when the maximum amount of queued buffers is reached. \nappsink \na GstApp.AppSink \n\ntrue if appsink is dropping old buffers when the queue is filled. \n"});