fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/gstappsink.html#gst_app_sink_get_emit_signals", "fragment": "GstApp.prototype.get_emit_signals \ngst_app_sink_get_emit_signals \ngst_app_sink_get_emit_signals \nCheck if appsink will emit the new preroll and new sample signals. \nParameters \na GstApp.AppSink \ntrue if appsink is emiting the new preroll and new sample signals. \nCheck if appsink will emit the new preroll and new sample signals. \nParameters \na GstAppSink \nTRUE if appsink is emiting the new preroll and new sample signals. \nCheck if appsink will emit the new preroll and new sample signals. \nParameters \na GstAppSink \nTRUE if appsink is emiting the new preroll and new sample signals. \n"});