fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/gstappsink.html#gst_app_sink_get_wait_on_eos", "fragment": "gst_app_sink_get_wait_on_eos \nGstApp.AppSink.get_wait_on_eos \nGstApp.prototype.get_wait_on_eos \nCheck if appsink will wait for all buffers to be consumed when an EOS is received. \nParameters \na GstAppSink \nTRUE if appsink will wait for all buffers to be consumed when an EOS is received. \nCheck if appsink will wait for all buffers to be consumed when an EOS is received. \nParameters \na GstApp.AppSink \nTrue if appsink will wait for all buffers to be consumed when an EOS is received. \nCheck if appsink will wait for all buffers to be consumed when an EOS is received. \nParameters \na GstApp.AppSink \ntrue if appsink will wait for all buffers to be consumed when an EOS is received. \n"});