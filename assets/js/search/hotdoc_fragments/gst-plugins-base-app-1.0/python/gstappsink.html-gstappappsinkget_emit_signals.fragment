fragment_downloaded_cb({"url": "gst-plugins-base-app-1.0/python/gstappsink.html#gstappappsinkget_emit_signals", "fragment": "GstApp.AppSink.get_emit_signals \nCheck if appsink will emit the new preroll and new sample signals. \nappsink \na GstApp.AppSink \n\nTrue if appsink is emiting the new preroll and new sample signals. \n"});