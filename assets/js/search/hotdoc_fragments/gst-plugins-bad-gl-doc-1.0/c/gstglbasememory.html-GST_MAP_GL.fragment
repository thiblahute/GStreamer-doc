fragment_downloaded_cb({"url": "gst-plugins-bad-gl-doc-1.0/c/gstglbasememory.html#GST_MAP_GL", "fragment": "<div class=\"base_symbol_container\" data-hotdoc-tags=\"\" id=\"GST_MAP_GL\">\n\t\t<h3 class=\"constant\" id=\"gst_map_gl\">\n\tGST_MAP_GL\n</h3>\n\n\t\t<pre class=\"raw_code\">\n#define GST_MAP_GL (GST_MAP_FLAG_LAST &lt;&lt; 1)\n</pre>\n\n<p>Flag indicating that we should map the GL object instead of to system memory.</p>\n<p>Combining <a href=\"../../gst-plugins-bad-gl-doc-1.0/c/gstglbasememory.html#GST_MAP_GL\">GST_MAP_GL</a> with <a href=\"../../libgstreamer-doc-1.0/c/gstmemory.html#GST_MAP_WRITE\">GST_MAP_WRITE</a> has the same semantics as though\nyou are writing to OpenGL. Conversely, combining <a href=\"../../gst-plugins-bad-gl-doc-1.0/c/gstglbasememory.html#GST_MAP_GL\">GST_MAP_GL</a> with\n<a href=\"../../libgstreamer-doc-1.0/c/gstmemory.html#GST_MAP_READ\">GST_MAP_READ</a> has the same semantics as though you are reading from OpenGL.</p>\n\n\n</div>\n\n\n\n"});