fragment_downloaded_cb({"url": "libgstcontroller-1.0/c/gstdirectcontrolbinding.html#gstdirectcontrolbinding-page", "fragment": "GstDirectControlBinding \nA value mapping object that attaches control sources to gobject properties. It will map the control values directly to the target property range. If a non absolute direct control binding is used the value range is mapped to full target property range and all values outside the range will be clipped. An absolute control binding will not do any value transformations. \n"});