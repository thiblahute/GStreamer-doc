fragment_downloaded_cb({"url": "gst-plugins-base-video-1.0/video-frame.html#GstVideoFrame", "fragment": "GstVideoFrame \nFields \nGstVideo.VideoFrame \nAttributes \nGstVideo.VideoFrame \nAttributes \nA video frame obtained from gst_video_frame_map \nthe GstVideoInfo \nGstVideoFrameFlags for the frame \nthe mapped buffer \npointer to metadata if any \nid of the mapped frame. the id can for example be used to indentify the frame in case of multiview video. \npointers to the plane data \nmappings of the planes \nA video frame obtained from GstVideo.VideoFrame.map \nthe GstVideo.VideoInfo \nGstVideo.VideoFrameFlags for the frame \nthe mapped buffer \npointer to metadata if any \nid of the mapped frame. the id can for example be used to indentify the frame in case of multiview video. \npointers to the plane data \nmappings of the planes \nA video frame obtained from GstVideo.prototype.map \nthe GstVideo.VideoInfo \nGstVideo.VideoFrameFlags for the frame \nthe mapped buffer \npointer to metadata if any \nid of the mapped frame. the id can for example be used to indentify the frame in case of multiview video. \npointers to the plane data \nmappings of the planes \n"});