fragment_downloaded_cb({"url": "gst-plugins-base-video-1.0/python/video.html#gstvideovideo_guess_framerate", "fragment": "Given the nominal duration of one video frame this function will check some standard framerates for a close match within and return one if possible \nIt will calculate an arbitrary framerate if no close match was found and return False. \nIt returns False if a duration of is passed. \n"});