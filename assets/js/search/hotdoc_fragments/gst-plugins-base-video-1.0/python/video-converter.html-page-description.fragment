fragment_downloaded_cb({"url": "gst-plugins-base-video-1.0/python/video-converter.html#page-description", "fragment": "Get the current configuration of convert. \nSet config as extra configuraion for convert. \nIf the parameters in config can not be set exactly this function returns False and will try to update as much state as possible. The new state can then be retrieved and refined with GstVideo.VideoConverter.get_config. \nLook at the GST_VIDEO_CONVERTER_OPT_ fields to check valid configuration option and values. \nonly perform chroma upsampling \nGstVideo.VideoChromaMode.downsample_only \nonly perform chroma downsampling \nGstVideo.VideoChromaMode.none \nuse the input color matrix to convert to and from R G B \nGstVideo.VideoMatrixMode.output_only \nuse the output color matrix to convert to and from R G B \nGstVideo.VideoMatrixMode.none \nDifferent primaries conversion modes \ndo conversion between primaries only when it can be merged with color matrix conversion. \nGstVideo.VideoPrimariesMode.fast \nGST_TYPE_RESAMPLER_METHOD The resampler method to use for resampling. Other options for the resampler can be used see the GstResampler. Default is GST_RESAMPLER_METHOD_CUBIC \nGST_TYPE_RESAMPLER_METHOD The resampler method to use for chroma resampling. Other options for the resampler can be used see the GstResampler. Default is GST_RESAMPLER_METHOD_LINEAR \nG_TYPE_UINT not introspectable The number of taps for the resampler. Default is let the resampler choose a good value. \nGST_TYPE_VIDEO_DITHER_METHOD The dither method to use when changing bit depth. Default is GstVideo.VideoDitherMethod.bayer. \nG_TYPE_UINT not introspectable The quantization amount to dither to. Components will be quantized to multiples of this value. Default is \nG_TYPE_INT not introspectable source x position to start conversion default \nG_TYPE_INT not introspectable source y position to start conversion default \nG_TYPE_INT not introspectable source width to convert default source width \nG_TYPE_INT not introspectable source height to convert default source height \nG_TYPE_INT not introspectable x position in the destination frame default \nG_TYPE_INT not introspectable y position in the destination frame default \nG_TYPE_INT not introspectable width in the destination frame default destination width \nG_TYPE_INT not introspectable height in the destination frame default destination height \nG_TYPE_BOOLEAN not introspectable if the destination rectangle does not fill the complete destination image render a border with GstVideo.VIDEO_CONVERTER_OPT_BORDER_ARGB. Otherwise the unusded pixels in the destination are untouched. Default True. \nG_TYPE_DOUBLE not introspectable the alpha color value to use. Default to \nGST_TYPE_VIDEO_ALPHA_MODE the alpha mode to use. Default is GstVideo.VideoAlphaMode.copy. \nG_TYPE_UINT not introspectable the border color to use if GstVideo.VIDEO_CONVERTER_OPT_FILL_BORDER is set to True. The color is in ARGB format. Default xff000000 \nGST_TYPE_VIDEO_CHROMA_MODE set the chroma resample mode subsampled formats. Default is GstVideo.VideoChromaMode.full. \nGST_TYPE_VIDEO_MATRIX_MODE set the color matrix conversion mode for converting between Y PbPr and non linear RGB R G B Default is GstVideo.VideoMatrixMode.full. \nGST_TYPE_VIDEO_GAMMA_MODE set the gamma mode. Default is GstVideo.VideoGammaMode.none. \nGST_TYPE_VIDEO_PRIMARIES_MODE set the primaries conversion mode. Default is GstVideo.VideoPrimariesMode.none. \nG_TYPE_UINT not introspectable maximum number of threads to use. Default for the number of cores. \n"});