fragment_downloaded_cb({"url": "gst-plugins-base-video-1.0/gstvideometa.html#gst_buffer_add_video_meta", "fragment": "prototype.buffer_add_video_meta \nGstVideo.buffer_add_video_meta \ngst_buffer_add_video_meta \nAttaches GstVideoMeta metadata to buffer with the given parameters and the default offsets and strides for format and width x height. \nThis function calculates the default offsets and strides and then calls prototype.buffer_add_video_meta_full with them. \nParameters \na Gst.Buffer \nGstVideo.VideoFrameFlags \na GstVideo.VideoFormat \nthe width \nthe height \nthe GstVideo.VideoMeta on buffer. \nAttaches GstVideoMeta metadata to buffer with the given parameters and the default offsets and strides for format and width x height. \nThis function calculates the default offsets and strides and then calls GstVideo.buffer_add_video_meta_full with them. \nParameters \na Gst.Buffer \nGstVideo.VideoFrameFlags \na GstVideo.VideoFormat \nthe width \nthe height \nthe GstVideo.VideoMeta on buffer. \nAttaches GstVideoMeta metadata to buffer with the given parameters and the default offsets and strides for format and width x height. \nThis function calculates the default offsets and strides and then calls gst_buffer_add_video_meta_full with them. \nParameters \na GstBuffer \nGstVideoFrameFlags \na GstVideoFormat \nthe width \nthe height \nthe GstVideoMeta on buffer. \n"});