fragment_downloaded_cb({"url": "gst-plugins-base-video-1.0/c/video-tile.html#GstVideoTileMode", "fragment": "Every four adjacent blocks two horizontally and two vertically are grouped together and are located in memory in Z or flipped Z order. In case of odd rows the last row of blocks is arranged in linear order. \n"});