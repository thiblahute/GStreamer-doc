fragment_downloaded_cb({"url": "gstreamer-core-doc-1.0/python/gstbufferpool.html#GstBufferPoolAcquireParams", "fragment": "<div class=\"base_symbol_container\" data-hotdoc-tags=\"\" id=\"GstBufferPoolAcquireParams\">\n\t\t<h3 id=\"gstbufferpoolacquireparams\">\n\tGst.BufferPoolAcquireParams\n</h3>\n\n\t\t<pre>\ntypedef struct _GstBufferPoolAcquireParams  Gst.BufferPoolAcquireParams</pre>\n<p>Parameters passed to the <a href=\"../../gstreamer-core-doc-1.0/python/gstbufferpool.html#gst_buffer_pool_acquire_buffer\">Gst.BufferPool.acquire_buffer</a> function to control the\nallocation of the buffer.</p>\n<p>The default implementation ignores the <em>start</em> and <em>stop</em> members but other\nimplementations can use this extra information to decide what buffer to\nreturn.</p>\n\n\n</div>\n\n\n\t"});