fragment_downloaded_cb({"url": "gstreamer-core-doc-1.0/python/gststreams.html#GstStream", "fragment": "<div class=\"base_symbol_container\" data-hotdoc-tags=\"\" id=\"GstStream\">\n\t\t<h3 id=\"gststream2\">\n\tGst.Stream\n</h3>\n\n\t\t<pre>\ntypedef struct _GstStream  Gst.Stream</pre>\n<p>A high-level object representing a single stream. It might be backed, or\nnot, by an actual flow of data in a pipeline (#GstPad).</p>\n<p>A <a href=\"../../gstreamer-core-doc-1.0/python/gststreams.html#GstStream\">Gst.Stream</a> does not care about data changes (such as decoding, encoding,\nparsing,...) as long as the underlying data flow corresponds to the same\nhigh-level flow (ex: a certain audio track).</p>\n<p>A <a href=\"../../gstreamer-core-doc-1.0/python/gststreams.html#GstStream\">Gst.Stream</a> contains all the information pertinent to a stream, such as\nstream-id, tags, caps, type, ...</p>\n<p>Elements can subclass a <a href=\"../../gstreamer-core-doc-1.0/python/gststreams.html#GstStream\">Gst.Stream</a> for internal usage (to contain information\npertinent to streams of data).</p>\n\n\n</div>\n\n\n\n"});