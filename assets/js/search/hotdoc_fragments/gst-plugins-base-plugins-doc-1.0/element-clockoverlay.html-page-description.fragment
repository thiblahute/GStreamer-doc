fragment_downloaded_cb({"url": "gst-plugins-base-plugins-doc-1.0/element-clockoverlay.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-clockoverlay\">\n<h1 id=\"clockoverlay1\">clockoverlay</h1><p>This element overlays the current clock time on top of a video\nstream. You can position the text and configure the font details\nusing the properties of the GstBaseTextOverlay class. By default, the\ntime is displayed in the top left corner of the picture, with some\npadding to the left and to the top.</p>\n<h2 id=\"example-launch-lines\">Example launch lines</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 -v videotestsrc ! clockoverlay ! autovideosink\n</code></pre>\n<p>Display the current wall clock time in the top left corner of the video picture</p>\n<pre><code class=\"language-[\"> gst-launch-1.0 -v videotestsrc ! clockoverlay halignment=right valignment=bottom text=\"Edge City\" shaded-background=true font-desc=\"Sans, 36\" ! videoconvert ! autovideosink\n</code></pre>\n<p>Another pipeline that displays the current time with some leading\ntext in the bottom right corner of the video picture, with the background\nof the text being shaded in order to make it more legible on top of a\nbright video background.</p>\n\n</div>\n\n\n"});