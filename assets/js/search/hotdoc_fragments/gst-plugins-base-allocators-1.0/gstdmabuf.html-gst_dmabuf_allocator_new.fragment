fragment_downloaded_cb({"url": "gst-plugins-base-allocators-1.0/gstdmabuf.html#gst_dmabuf_allocator_new", "fragment": "gst_dmabuf_allocator_new \nGstAllocators.DmaBufAllocator.new \nGstAllocators.DmaBufAllocator.prototype.new \nReturn a new dmabuf allocator. \na new dmabuf allocator or NULL if the allocator isn t available. Use gst_object_unref to release the allocator after usage \nSince \nReturn a new dmabuf allocator. \na new dmabuf allocator or NULL if the allocator isn t available. Use Gst.Object.unref to release the allocator after usage \nSince \nReturn a new dmabuf allocator. \na new dmabuf allocator or NULL if the allocator isn t available. Use Gst.Object.prototype.unref to release the allocator after usage \nSince \n"});