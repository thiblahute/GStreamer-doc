fragment_downloaded_cb({"url": "gst-plugins-ugly-1.0/element-xingmux.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-xingmux\">\n<h1 id=\"xingmux\">xingmux</h1><p>xingmux adds a Xing header to MP3 files. This contains information about the duration and size\nof the file and a seek table and is very useful for getting an almost correct duration and better\nseeking on VBR MP3 files.</p>\n<p>This element will remove any existing Xing, LAME or VBRI headers from the beginning of the file.</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 audiotestsrc num-buffers=1000 ! audioconvert ! lamemp3enc ! xingmux ! filesink location=test.mp3\n gst-launch-1.0 filesrc location=test.mp3 ! xingmux ! filesink location=test2.mp3\n gst-launch-1.0 filesrc location=test.mp3 ! mp3parse ! xingmux ! filesink location=test2.mp3\n</code></pre>\n\n</div>\n\n\n"});