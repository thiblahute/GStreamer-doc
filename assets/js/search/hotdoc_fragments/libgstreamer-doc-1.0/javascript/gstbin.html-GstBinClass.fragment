fragment_downloaded_cb({"url": "libgstreamer-doc-1.0/javascript/gstbin.html#GstBinClass", "fragment": "<div class=\"base_symbol_container\" data-hotdoc-tags=\"\" id=\"GstBinClass\">\n\t\t<h3 id=\"gstbinclass\">\n\tGst.BinClass\n</h3>\n\n\t\t<pre>\ntypedef struct _GstBinClass  Gst.BinClass</pre>\n<p>Subclasses can override the <em>add_element</em> and <em>remove_element</em> to\nupdate the list of children in the bin.</p>\n<p>The <em>handle_message</em> method can be overridden to implement custom\nmessage handling.  <em>handle_message</em> takes ownership of the message, just like\n<a href=\"../../libgstreamer-doc-1.0/javascript/gstelement.html#gst_element_post_message\">Gst.Element.prototype.post_message</a>.</p>\n<p>The <em>deep_element_added</em> vfunc will be called when a new element has been\nadded to any bin inside this bin, so it will also be called if a new child\nwas added to a sub-bin of this bin. <a href=\"../../libgstreamer-doc-1.0/javascript/gstbin.html#GstBin\">Gst.Bin</a> implementations that override\nthis message should chain up to the parent class implementation so the\nelement-added-deep signal is emitted on all parents.</p>\n\n\n</div>\n\n\n\t"});