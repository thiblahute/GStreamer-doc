fragment_downloaded_cb({"url": "gst-plugins-good-1.0/element-smpte.html#smpte", "fragment": "smpte \nsmpte can accept I420 video streams with the same width height and framerate. The two incomming buffers are blended together using an effect specific alpha mask. \nThe GstSmpte depth property defines the presision in bits of the mask. A higher presision will create a mask with smoother gradients in order to avoid banding. \n"});