fragment_downloaded_cb({"url": "gst-plugins-good-1.0/element-shout2send.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"element-shout2send\">\n<h1 id=\"shout2send1\">shout2send</h1><p>shout2send pushes a media stream to an Icecast server</p>\n<h2 id=\"example-launch-line\">Example launch line</h2>\n<pre><code class=\"language-[\"> gst-launch-1.0 uridecodebin uri=file:///path/to/audiofile ! audioconvert ! vorbisenc ! oggmux ! shout2send mount=/stream.ogg port=8000 username=source password=somepassword ip=server_IP_address_or_hostname\n ]| This pipeline demuxes, decodes, re-encodes and re-muxes an audio\n media file into oggvorbis and sends the resulting stream to an Icecast\n server. Properties mount, port, username and password are all server-config\n dependent.\n</code></pre>\n\n</div>\n\n\n"});