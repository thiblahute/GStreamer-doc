fragment_downloaded_cb({"url": "gst-plugins-good-1.0/element-deinterleave.html#keeppositions", "fragment": "Keep positions When enable the caps on the output buffers will contain the original channel positions. This can be used to correctly interleave the output again later but can also lead to unwanted effects if the output should be handled as Mono. \nFlags Read Write \nDefault value false \n"});